services:
  - type: web
    name: quizarena-api
    runtime: node
    buildCommand: npm ci && npm run db:generate && npm run db:migrate:deploy && npm run build:server
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: REDIS_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: WEB_APP_URL
        sync: false
